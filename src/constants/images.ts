/**
 * Image-related constants.
 *
 * A single neutral blurhash is used as the placeholder for all remote images so
 * cards/hero fade in from a soft blur instead of popping in. (In a real app each
 * course would carry its own blurhash from the CMS.)
 */

export const BLURHASH = 'L6Pj0^i_.AyE_3t7t7R**0o#DgR4';

/** Default cross-fade duration (ms) for expo-image transitions. */
export const IMAGE_TRANSITION_MS = 300;
